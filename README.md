# Current Location Generator

  - uses pure javascript to get coordinates and manually generate url for google maps

## how to
  - include `current-location-generator.js` into `head` tag of html file
